# Superior Download

画像URLリストから画像を一括ダウンロードするFlaskアプリ。

Part of the **Superior Series** — a set of tools for image management workflows.

## 起動方法

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```
